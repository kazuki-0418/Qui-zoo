import express from "express";

const router = express.Router();

// TODO All routes require authentication

// Quiz routes
// router.post("/", sessionController.createRoom);
// router.get("/", sessionController.getRooms);
// router.get("/validate/:room_code", sessionController.validateRoomCode);

export default router;
